import React, { PropsWithChildren } from "react";
import styled from "styled-components";

const Img = styled.image``;
// Q styled component props 상속

type Props = PropsWithChildren<{}>;

const Image = (props: Props) => {
  return;
};

export default Image;
